
Установка (Arch + Hyprland)

Если ты здесь, значит тебе понравился мой рабочий стол из того видео в тиктоке и ты хочешь такой же. Сразу предупреждаю: это не операционная система Windows. Это Arch Linux с кастомным интерфейсом.
🛑  ДИСКЛЕЙМЕР
Если ты не программист, не пентестер, не системный администратор и вообще не привык пользоваться терминалом — закрой эту страницу.
Если ты пришел сюда только ради «красивых кнопочек» и анимаций, но твой максимум — это установка игр из Steam или браузинг в Chrome, этот сетап тебя разьебет. В Linux (а особенно в Arch) всё ломается, если ты не понимаешь, что делаешь. Здесь нет техподдержки, здесь нет кнопки «сделать всё красиво и чтобы работало».
Хочешь красивый интерфейс без боли? Поставь тему на Windows или купи Mac. Линукс — это инструмент, а не игрушка.

Этот райс основан на проекте [dots-hyprland от end-4](https://github.com/end-4/dots-hyprland). Здесь собрано всё: анимации, виджеты, удобные меню, шрифты, и т.д
🛑 Шаг 0: Важные условия
1. **Операционная система:** Только **Arch Linux**. На Ubuntu, Mint или Windows и MacOS это работать не будет.
2. **Видеокарта:** 
   - Если у тебя **Intel** или **AMD** — всё будет летать «из коробки».
   - Если у тебя **NVIDIA**(как у меня)— приготовься к возможным приколам (нужно будет ставить драйверы и прописывать параметры запуска, читай [Hyprland Wiki](https://wiki.hyprland.org/Nvidia/)). тебе нужно будет добавить в /etc/mkinitcpio.conf в строку MODULES=(...) внутрь скобок nvidia nvidia_modeset nvidia_uvm nvidia_drm и пересобрать образ ядра, найди инфу про это сам, мне похуй <3

     
3. **Чистая система:** Лучше всего ставить это на свежеустановленный Arch (без других оболочек типа KDE или GNOME).
Загрузись с флешки: В BIOS выбери загрузку с USB на которую установил ISO-образ
Что бы работал Wi-Fi, напиши: iwctl, затем device list, затем station wlan0 connect SSID (вместо SSID имя твоего вайфая). потом пароль, и попробуй пингануть гугл ping google.com, если пинг идет пиши archinstall, дальше разберешься сам.

## 🛠 Шаг 1: Подготовка системы

Сначала обнови систему и установи базовые инструменты:

```bash
sudo pacman -Syu
sudo pacman -S --needed git base-devel
```

установи **AUR-помощник** (я рекомендую `yay`). Если его нет, установи:

```bash
git clone https://aur.archlinux.org/yay-bin.git
cd yay-bin
makepkg -si
cd ..
rm -rf yay-bin
```

---

## Шаг 2: Установка райса (Автоматически)

Автор конфига (end-4) сделал удобный скрипт, который сам скачает все нужные шрифты, иконки и зависимости тебе нужно будет всего-лишь отвечать на вопросы по ходу установки

1. **Клонируй репозиторий:**
   ```bash
   git clone https://github.com/end-4/dots-hyprland.git
   cd dots-hyprland
   ```

2. **Запусти установщик:**
   ```bash
   ./install.sh  
   ```
   или ./setup.sh я уже забыл, сам разберешься

3. **Что выбирать в процессе?**
   - Скрипт будет задавать вопросы. Внимательно читай их.
   - Если спросит, устанавливать ли зависимости — жми **Y** (Yes).
   - Если предложит выбрать пакеты, обычно можно оставлять по умолчанию.

---

##  Шаг 3: Как получить именно "мой" вид?

Поскольку я настраивал этот райс под себя, после основной установки тебе нужно применить мои конфиги:

Которые я не дам, мне впадлу собирать конфиги и пушить их и есть вероятность что спалю данные и АПИ ключи

---

## ⌨️ Горячие клавиши (Шпаргалка)

После перезагрузки и входа в Hyprland, вот основные кнопки:

*   `Super` (клавиша Windows) + `Enter` — Терминал (Foot / Kitty)
*   `Super` + `Q` — Закрыть окно
*   `Super` + `A` — Открыть меню приложений (через виджеты AGS)
*   `Super` + `E` — Файловый менеджер
*   `Super` + `V` — Буфер обмена
*   `Super` + `T` — Telegram
*   `Super` + `W` — Thorium  (браузер который у меня) 
*   `Super` + `L` — Заблокировать экран
*   `Super` + `M` — Меню питания (выключить/перезагрузить)
*   `Super` + `/`— Посмотреть все горячие клавиши (в этом райсе встроен крутой хелпер)
*   `Super` + `Цифры` — перемешаться между рабочими столами, как делал я в видео

---

## FAQ (Частые вопросы которые меня заебали)

**1. Почему вместо текста квадратики?**
Тебе нужно установить шрифты. Обычно скрипт это делает, но если нет — поставь `JetBrainsMono Nerd Font` и `Noto Sans CJK`.

**2. У меня черный экран после входа!**
Скорее всего, у тебя NVIDIA. Нужно добавить параметры `nvidia_drm.modeset=1` в конфиг загрузчика (GRUB/systemd-boot). sudo nano /etc/modprobe.d/nvidia.conf и добавить options nvidia_drm modeset=1

**3. Как сменить обои?**
Нажми на значок настроек/палитры на панели или используй меню по `Super + A`.
---

**ГЛ! И не спрашивайте меня, как поменять цвет полосочки — всё есть в официальной документации Hyprland и конфигах в папке `~/.config/hypr/` и больше не пишите мне никуда, я не отвечу никому, блядский Тик Ток решил выпустить мой рофлоролик в реки всяким даунам и теперь везде в личных сообщениях мусор, скажите спасибо что сделал хотя бы этот гайд для вас.**
